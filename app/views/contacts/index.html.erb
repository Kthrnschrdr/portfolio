<h1>All Contacts</h1>
<div class="contacts">
  <% @contacts.each do |contact| %>
    <p><%= link_to contact.name, contact_path(contact.id) %></p>
  <% end %>
</div>

<div class="bottom_link"><%= link_to "New Contact", new_contact_path %></div>