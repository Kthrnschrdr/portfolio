<div class="links">
  <% @links.each do |link| %>
    <p><%= link_to link.name, link_path(link.id) %></p>
  <% end %>
</div>
<% if current_user %>
	<div class="bottom_link"><%= link_to "New Link", new_link_path %></div>
<% end %>